<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>RPS</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
  </head>
  <body>
    <h1>
      <%= `${name}'s Choice: ${weapon}` %>
    </h1>
    <h1>
      <%= `Computer's Choice: ${computerChoice}` %>
    </h1>
    <h1><%= `Result: ${result}` %></h1>
    <h1>
      <%= `Winning Streak: ${streak}` %>
    </h1>

    
    <form action="/game" method="get">
    <input class="btn btn-primary" id="replay-button" type="submit" value="Play Again!" name="replay">
    </form>
    <form action ="/" method="get">
      <input style="margin-top: 10px;" class="btn btn-primary" id="exit-button" type="submit" value="Exit Game" name="exit">
     </form>
  </body>
</html>
